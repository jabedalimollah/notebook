
index.js
    database connection/connectDB
    app listen port or running port
    dotenv config

app.js
    express
    cors
    cookieparser
    bodyparser
    app
    /routes/user.routes.js
        signup,login (app.use("/api/v1/users", userRouter))
    
    
/routes
    user.routes.js
    express
    express.Router()
    /constrollers/user.constroller.js
        signup
        login (ex.- router.route("/login").post(loginUser) )

/constrollers
    /models/user.model.js
        user
    signup hangler function
    login handler function

/models
    mongoose
    bcrypt 
    jwt
    userSchema

/schemas
    signup.schema.js (using zod)

/db
    mongoose
    database connection/ mongoose.connect / connectDB
    dotenv

/middlewares
    middlewares(ex.- jwt)
    validation.middleware.js
    
/utils
    api error handle
    api response handle 
    async handler 

/public


.env file 
    PORT 
    MONGODB_URI
    DB_NAME
    etc 

.env.sample
    blank 
         PORT 
        MONGODB_URI
        DB_NAME
        etc 

.gitignore
    .env
    node_modules/